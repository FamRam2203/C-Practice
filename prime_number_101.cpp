#include <iostream>
using namespace std;

int main()
{
    int a,b,c,d;
    //int n=[2,3,4,5,6,7,8,9];
    cout<<"Enter number: ";
    cin>>a;
    if(a%2==0 || a%3==0 || a%5==0 || a%7==0 || a%11==0 || a%13==0 || a%17==0 || a%19==0 || a%23==0 || a%29==0 || a%31==0 || a%37==0 || a%41==0 || a%43==0 || a%47==0 || a%53==0 || a%59==0 || a%61==0 || a%67==0 || a%71==0 || a%73==0 || a%79==0 || a%83==0 || a%89==0 || a%97==0)
    {
        cout<<"The number is a non prime number..."<<endl;
    }
    else
    {
        cout<<"The given number is a prime number..."<<endl;
    }

    //if(a/a==1 || )

}
